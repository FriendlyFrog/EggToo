extends Sprite
var hatTypes = [load("res://Assets/CowboyFace.png")]
var activeHat 
var playerHealth = 100
var enemyHealth = 100
var playerActionPoints
# class member variables go here, for example:
# var a = 2
# var b = "textvar"

func _ready():
	#activeHat = hatTypes[0].instance()
	#add_child(activeHat)
	pass


func _process(delta):
	# Called every frame. Delta is time since last frame.
	# Update game lxx
	pass